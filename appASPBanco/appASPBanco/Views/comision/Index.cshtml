@model IEnumerable<appASPBanco.Models.comision>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreComision)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.montoComision)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreComision)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.montoComision)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idComision }) |
            @Html.ActionLink("Details", "Details", new { id=item.idComision }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idComision })
        </td>
    </tr>
}

</table>
